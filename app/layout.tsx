import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Otto — The strength data your Apple Watch is missing",
  description:
    "Otto makes the invisible work of lifting visible. Velocity-based strength metrics — Effort, Training Load, and Stimulus — on the Apple Watch you already own. No extra hardware.",
  openGraph: {
    title: "Otto — Make the invisible work of lifting visible",
    description:
      "Velocity-based strength training metrics on the Apple Watch you already own. No bar sensors, no extra band.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col bg-background text-foreground">
        {children}
      </body>
    </html>
  );
}
